var printCount = 5;

function printMessage(index) {
    var msg = `Hello Falcon Punch ${index}!`;
    console.log(msg);
}

for(let i = 0; i < printCount; i++) {
    printMessage(i);
}

//Test pushing code